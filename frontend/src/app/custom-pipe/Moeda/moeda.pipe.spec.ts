/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MoedaPipe } from './moeda.pipe';

describe('Pipe: Moedae', () => {
  it('create an instance', () => {
    let pipe = new MoedaPipe();
    expect(pipe).toBeTruthy();
  });
});
